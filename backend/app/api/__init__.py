# This file can be empty.
# Its presence marks the 'api' directory as a Python package,
# which allows us to import the routers defined within this directory
# (e.g., products.py, users.py) into other parts of the application,
# such as the main.py file where the FastAPI app is created.
